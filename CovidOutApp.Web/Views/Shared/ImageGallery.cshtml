@model IEnumerable<CovidOutApp.Web.ViewModels.VenueImageViewModel> 

 <div id="carouselExampleControls" class="carousel slide" data-ride="carousel" style="background-color:gray">
  <div class="carousel-inner">

      @for (int i=0; i< Model.Count(); i++)
      { 
          var className = "carousel-item";
            
          if (i == 0)
              className += " active";
          
           <div class="@className">
                 <img class="d-block w-100" src="@Model.ElementAt(i).FilePath" alt="@Model.ElementAt(i).Title">
           </div>
      }
</div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>   